const operators = ['+', '-', '*', ' / '];

const add = (a,b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => {
    if (b ==0) {
        return "Error: you cant do that"
    }
    else {
        return a / b
    }
}
let


let number1 = 
let operator =
let number2 = 
let displayValue = 

